# 当前最佳模型说明与验证报告

日期：2026-08-25

评估口径：官方 Weighted Zero-Mean R2，本地严格时间前推验证

## 一、结论

截至 2026-08-25，项目中**本地验证分数最高**的模型是：

> **Regime 复合模型 + XGBoost 残差修正 + 辅助状态堆叠残差 + 因果时序 Ridge 残差**

它在最近 300,000 行训练期外层验证集上的 Weighted Zero-Mean R2 为：

```text
0.0064196889
```

该结果高于原始 Regime 复合模型的 `0.0060421867`，绝对提升
`0.0003775023`。完整提交包含 3,217,458 行，已经通过独立结构审计。

但必须区分三种状态：

| 状态 | 模型 | 本地分数 | 结论 |
|---|---|---:|---|
| 当前本地分数最高 | 四层融合模型 | 0.0064196889 | 可作为线上 A/B 候选 |
| 次优本地候选 | 去掉最后时序层 | 0.0064109624 | 增量更大，历史折证据更充分 |
| 当前稳定推荐基线 | Regime + XGBoost 残差 | 0.0061247598 | 已完整复现和审计，结构更简单 |

**目前没有可核验的线上排行榜分数。**因此本文所说的“最好”只表示当前项目内、同一验证口径下的本地最高分，不能表述为线上最优或比赛最优。

## 二、从数据到最终预测的完整步骤

最终模型不是把几个模型一次性平均，而是先构造一个多视角主模型，再逐层检查新的信号能否解释主模型尚未解释的误差。只有在更晚时间段仍有增量的信号才进入下一层。

完整数据流为：

```text
原始 feature
  |
  +--> 多窗口逐行 Ridge/LightGBM --> 横截面均值调整 --------+
  |                                                          |
  +--> Panel 市场头 + 资产相对头 ----------------------------+--> 三路基础融合
  |                                                          |        |
  +--> Market32 市场聚合 LightGBM ---------------------------+        |
  |                                                                   +--> Exact 条件修正
  +--> 市场状态 Logistic + 条件 Ridge 幅度头 -------------------------+        |
                                                                              +--> Regime 主预测 R
更早训练的 XGBoost ------------------------------------------------------------+--> 残差基线 B
feature -> predicted weight/responders -> target ------------------------------+--> 辅助候选 A
asset/market 历史 lag/delta/rolling -> Ridge 残差 -----------------------------+--> 最终预测 F
```

最终四层公式为：

```text
R = Regime 复合预测
B = R - 0.1860647991 * XGBoost预测
A = B - 0.4363287926 * 辅助状态增量
F = A - 0.0334517180 * 因果时序Ridge残差信号
```

下面按实际计算顺序解释每一步。

### 第 0 步：明确数据、目标和时间边界

训练行包含 `row_id`、`time_id`、`asset_id`、323 个匿名 feature、`target`、`weight` 和 47 个 responder。测试行没有 `target`、`weight` 和 responder。

各字段在流程中的用途如下：

| 字段 | 用途 | 测试时是否可见 |
|---|---|---|
| `row_id` | 对齐预测和提交顺序，不作为信号 | 是 |
| `time_id` | 保证时间前推切分、构造历史窗口 | 是 |
| `asset_id` | 区分 15 个资产、构造资产历史和按资产 shrink | 是 |
| `feature_000..322` | 所有模型的可见输入 | 是 |
| `target` | 训练最终目标 | 否 |
| `weight` | 训练样本权重和官方评分权重 | 否 |
| `responder_00..46` | 仅作为辅助监督信号 | 否 |

主模型的大部分近期分支使用最后 75,000 个训练时点 `813480..888479`，其中 `868480..888479` 的 20,000 个时点作为近期 calibration。共有 15 个资产，因此这段 calibration 对应 300,000 行。

最关键的约束是：**任何需要选择权重、shrink 或残差系数的步骤，都只能在更早时间段完成，然后把参数冻结到更晚时间段。**不能随机打乱数据，也不能用 test 反调参数。

### 第 1 步：筛选稳定 feature，并建立两种观察尺度

模型不是直接把 323 个 feature 全部塞进每个下游模型，而是使用历史前向筛选得到的稳定 feature 排名。

后续主要形成两种输入：

1. **逐行输入**：一行代表某一时点的某一资产，适合预测个体资产 target。
2. **市场聚合输入**：把同一个 `time_id` 下最多 15 个资产的 feature 汇总成均值、标准差、最小值、最大值和极差，适合描述整个市场的共同状态。

这两种尺度互补。逐行模型容易发现个体差异，但可能忽略市场共同方向；市场聚合模型能看到整体状态，但不能单独解释每个资产的差异。

### 第 2 步：训练多窗口逐行模型

Neutralized 分支的原始输入不是一个模型，而是五个不同训练窗口的模型：

| 子模型 | 训练窗口 | 主要结构 |
|---|---:|---|
| `final120k` | 120,000 时点 | 项目早期最稳的逐行主模型，最终做全局 shrink |
| `residual90k` | 90,000 时点 | 48 feature Ridge + LightGBM 残差 |
| `residual80k` | 80,000 时点 | 48 feature Ridge + LightGBM 残差 |
| `residual75k` | 75,000 时点 | 48 feature Ridge + LightGBM 残差 |
| `residual60k` | 60,000 时点 | 48 feature Ridge + LightGBM 残差 |

四个 residual 模型的训练方式相同：

1. 用加权 Ridge 先学习 target 中较稳定的线性部分，Ridge `alpha=100`。
2. 计算训练残差 `target - ridge_prediction`。
3. 用强正则、小学习率的 LightGBM 学习剩余非线性部分。
4. 在 calibration 中选择 LightGBM residual weight 和按资产 shrink。
5. 固定选择后，用完整可用训练段重训，再预测 official test。

使用不同窗口的原因是市场关系会漂移。长窗口方差小但可能包含过时状态；短窗口更贴近当前市场但噪声更大。五个模型的预测不是等权平均，而是对每个资产分别使用带上下限的 SLSQP 优化权重。

约束包括：`final120k` 权重不低于 0.2，其他残差模型权重不高于 0.7，随后再做按资产 shrink。该五路融合在 calibration 上的全段分数为 `0.00415709`。

### 第 3 步：对逐行融合做同时间横截面均值调整

五路逐行融合后，模型检查同一 `time_id` 下 15 个资产预测的平均值是否存在系统偏差。计算式为：

```text
adjusted_prediction
= original_prediction - alpha * mean_prediction_at_same_time
```

calibration 最终选择 `alpha=-3.225`。代入公式后等价于：

```text
adjusted_prediction
= original_prediction + 3.225 * mean_prediction_at_same_time
```

因此虽然历史目录名叫 `neutralized`，这一版实际并不是简单地“减掉市场均值”，而是在近期 calibration 中发现原模型低估了共同市场方向，于是把同时间横截面均值加回来。之后再做按资产 shrink，shrink cap 为 0.5。

该分支单独的 calibration 分数为 `0.00498749`。它后续记为 `neutralized_prediction`，名称只是保留项目历史命名。

### 第 4 步：训练 Panel market-relative 分支

Panel 分支同时建模“市场整体”和“资产相对市场的偏离”。

输入包括：

- 48 个稳定 raw feature；
- 前 16 个 feature 的同时间横截面统计；
- 逐行和时间级衍生输入。

它包含两个头：

1. **Market head**：Ridge 与 LightGBM 按 50/50 组合，预测同一时点的市场共同 target。
2. **Relative head**：先用 Ridge 预测资产相对市场的部分，再用 LightGBM 学 Ridge 残差。

calibration 最终选择：

```text
panel_raw = 0.75 * market_head + 1.75 * relative_head
```

然后按资产做 shrink。这里权重和不要求等于 1，因为最后的 shrink 会统一校准输出幅度。该分支单独的 calibration 分数为 `0.00389384`。

### 第 5 步：训练 Market32 市场聚合分支

Market32 选择排名靠前的 32 个 raw feature。对每个 `time_id`，分别计算这 32 个 feature 在 15 个资产上的：

```text
mean / std / min / max / range
```

因此形成 `32 * 5 = 160` 个时间级市场输入。训练目标是按 `weight` 聚合后的市场 target。

模型使用两棵不同复杂度的 LightGBM 集成：

```text
95% * LightGBM(num_leaves=7,  min_child_samples=300, lambda=300)
+ 5% * LightGBM(num_leaves=15, min_child_samples=300, lambda=100)
```

小树占 95% 是为了控制方差，稍大的树只提供少量非线性补充。时间级预测再映射回同一时点的所有资产，并使用 calibration 中学到的按资产 shrink。该分支单独的 calibration 分数为 `0.00356450`。

### 第 6 步：把前三个视角融合为基础预测

三个分支分别代表：

- `neutralized`：多窗口逐行表格信号及共同方向调整；
- `panel`：资产相对市场的偏离；
- `Market32`：市场整体状态。

calibration 搜索后得到：

```text
threeway_raw
= 0.54 * neutralized_prediction
+ 0.22 * panel_prediction
+ 0.24 * Market32_prediction
```

再按每个资产分别做 shrink，shrink 上限为 1.4。这样做是因为不同资产 target 的可预测幅度不同，同一全局缩放会让部分资产过度预测、部分资产过度收缩。

三路融合后的 calibration 分数为 `0.00542162`，明显高于任意单个分支，说明三种观察尺度确实提供了互补信息。

### 第 7 步：只在强市场信号时启用 Exact-Market

Exact-Market 是一个高偏差、低方差的市场模型：

- 只使用排名前 8 个精确市场聚合特征；
- 对输入标准化；
- 用 Ridge `alpha=10000` 预测市场 target；
- 把时间级预测映射回所有资产；
- 再做按资产 shrink。

它不在所有行上强行参与融合。只有当：

```text
abs(exact_prediction) >= 0.0470988438
```

才执行：

```text
base_conditional
= 0.60 * threeway_prediction
+ 0.40 * exact_prediction
```

否则保持 `base_conditional = threeway_prediction`。official test 中约 36.77% 的行触发了该条件。

这一步的目的不是让 Exact-Market 主导，而是在它明确给出较强市场方向时，用低方差线性信号纠正三路基础模型。

### 第 8 步：识别市场状态，并预测相应幅度

市场状态模型先把每个训练时点的 15 个资产 target 转成三分类标签：

| 类别 | 定义 |
|---|---|
| 强负状态 | 正 target 的资产数不超过 3 |
| 混合状态 | 正 target 的资产数为 4 到 11 |
| 强正状态 | 正 target 的资产数不少于 12 |

然后使用 32 个市场聚合特征训练：

1. `LogisticRegression(C=0.01)`，输出三个状态的概率；
2. 三个 Ridge 幅度头，`alpha=10000`；
3. 强负头预测负状态下的绝对幅度，混合头直接预测市场 target，强正头预测正状态下的绝对幅度。

训练分类器时对三个类别重新平衡样本权重，避免模型只预测最常见的混合状态。最终使用软概率，不做生硬的 argmax 切换：

```text
regime_market_prediction
= -P(强负) * 负状态预测幅度
   +P(混合) * 混合状态预测值
   +P(强正) * 正状态预测幅度
```

最终 Regime 主模型为：

```text
R = base_conditional + 0.20 * regime_market_prediction
```

这里的 0.20 是残差式附加权重，不是把两者归一化为 80/20。完成这一步后，Regime 主模型的 outer 全段分数为 `0.0060421867`。

### 第 9 步：用更早训练的 XGBoost 修正 Regime 残差

XGBoost 来自独立策略包 `Quant-main.zip`，只读取 `feature_*` 和缺失模式指标。其模型训练在更早的 train partition 0-6，partition 7-8 用作其历史验证，因此它与近期 Regime 模型具有不同的训练窗口和模型偏差。

首先在最近 calibration 的每一行同时生成：

```text
target
weight
regime_prediction
xgb_prediction
```

然后把 calibration 按时间切成两半。只在前半段 `868480..878479` 拟合残差系数：

```text
beta
= sum(weight * xgb_prediction * (target - regime_prediction))
   / sum(weight * xgb_prediction^2)
```

该式是在加权平方误差下，求 XGBoost 信号解释 Regime 剩余误差的闭式最优系数。为防止偶然的大系数，beta 被限制在 `[-0.25, 0]`；最终原始拟合值和截断后值都为 `-0.1860647991`。

系数拟合后不再修改，直接用于后半段 `878480..888479`：

```text
B = R - 0.1860647991 * xgb_prediction
```

XGBoost 单独的全段分数只有 `0.0000728169`。它能进入最终模型，是因为它对 Regime 的误差有增量，而不是因为它单独很强。该层使全段分数从 `0.0060421867` 提升到 `0.0061247598`。

### 第 10 步：用 feature 预测测试时不可见的辅助状态

训练数据提供 `weight` 和 47 个 responder，但 official test 不提供。为了利用这些变量，只能训练一个从可见输入到辅助状态的模型：

```text
323 features + asset_id
        -> predicted weight + predicted responder_00..46
```

时间协议先把辅助模型和 target 模型完全隔开：

```text
aux train       688480..776479
purge gap       776480..777479
aux valid       777480..787479
purge gap       787480..788479
target train    788480..846479
purge gap       846480..847479
calibration     847480..867479
purge gap       867480..868479
outer validate  868480..888479
```

辅助输入先按 aux-train 的均值和标准差归一化，再拼接 15 维 `asset_id` one-hot。输入维度为 `323 + 15 = 338`。

同时训练两个模型：

1. **多输出 Ridge**：`alpha=1000`，一次输出 48 个标准化辅助变量。
2. **共享 MLP**：`338 -> 256 -> 128 -> 48`，激活为 GELU，dropout 为 0.05，使用 aux-valid 早停。

两个模型只在 aux-train 看到真实 weight/responders。它们在后续 target-train、calibration 和 outer 上只能输出预测值，不能再读取真实辅助变量作为输入。

这一步的价值在于把 323 个匿名 feature 压缩成“预计流动性、预计摩擦状态、预计方向响应”等低维状态。需要注意：真实 responder 与 target 相关，不代表 predicted responder 一定有用；所以后续仍要做时间外 target 验证。

### 第 11 步：训练二阶段 target 模型，并提取辅助状态增量

后续比较了多种 target 输入：

- 48 个 raw feature；
- 仅 predicted weight；
- 流动性 responder 组；
- 方向 responder 组；
- Ridge 预测出的全部辅助状态；
- MLP 预测出的全部辅助状态；
- raw 与各辅助状态的组合。

每个候选都采用同一训练协议：

1. Ridge `alpha=100` 学主要 target 信号；
2. LightGBM 尝试学习 Ridge 残差；
3. residual weight 只在 `{0, 0.25, 0.5, 0.75, 1.0}` 中选择；
4. 对输出求加权最优 shrink；
5. 以 calibration 前半和后半分数的较小值作为 selection score，避免只迎合其中一半。

最终选择 `predicted_aux_only`：输入为辅助 Ridge 的 48 个预测和辅助 MLP 的 48 个预测，共 96 维。其内部最优 residual weight 为 0，shrink 为 `0.84741337`。这表示在该分支上，强收缩 Ridge 比继续叠加 LightGBM 更稳。

冻结结构、residual weight 和 shrink 后，把 target-train 与 calibration 合并重训，再预测更晚的 outer。

为了避免把一个较弱二阶段模型直接替换强基线，最终只提取它相对 raw-feature control 的变化：

```text
auxiliary_delta
= refit_two_stage_prediction - refit_raw_feature_control_prediction
```

再在 outer 前半段拟合该增量对当前 XGBoost 残差基线的系数，冻结到后半段：

```text
A = B - 0.4363287926 * auxiliary_delta
```

负号只说明这段 `two_stage - raw control` 与强基线的剩余误差呈反向关系，不代表 weight 或 responder 在金融意义上是负向信号。

该层在 3/3 个历史前向折和最新冻结后半段均有正增量，使全段分数从 `0.0061247598` 提升到 `0.0064109624`。

### 第 12 步：构造严格因果的资产历史和市场历史

前三层主要使用当前行或当前时点横截面。最后一层显式加入历史：

- 48 个稳定 raw feature 作为当前值控制输入；
- 对前 16 个 feature 构造每个资产自身的历史；
- 对前 8 个 feature 构造市场聚合历史；
- lag 为 `1/4/16/32`；
- rolling 窗口为 `8/32`。

对每个资产 feature，具体构造：

```text
lag_k       = 同一资产恰好 k 个 time_id 之前的值
delta_k     = 当前值 - lag_k
rollmean_w  = 当前行以前 w 个值的均值
rollstd_w   = 当前行以前 w 个值的标准差
rolldev_w   = 当前值 - rollmean_w
```

lag 不仅执行行 shift，还检查真实 `time_id` 差是否等于 k，避免资产缺行时把不连续记录误当成连续历史。rolling 先 `shift(1)`，因此当前行不会进入自己的历史均值和标准差。

市场历史先在同一个 `time_id` 对最多 15 个资产求 feature mean/std，再对市场 mean 构造 lag、delta、rolling。最终时序残差模型只读取名称中带 lag/delta/rolling 的 336 个历史输入，不读取未来统计，也不使用当前时点横截面 rank。

### 第 13 步：训练时序 Ridge 残差，并冻结最后系数

时序候选先用当前 48 个 raw feature 训练 Ridge `alpha=100`，得到低方差控制预测：

```text
raw_residual = target - current_feature_ridge_prediction
```

再用强正则 Ridge `alpha=10000`，从 336 个历史输入预测 `raw_residual`。选择 Ridge 而不是 LightGBM，是因为实验中高维时序 LightGBM 的冻结后半段表现明显下降；共享 Ridge 虽然增量小，但方向更稳定。

在 outer 内再次使用三段时间前推：

```text
拟合最终系数  868480..872479
purge gap     872480..873479
独立校准      873480..877479
purge gap     877480..878479
最终 holdout  878480..888479
```

最早一段拟合得到：

```text
temporal_gamma = -0.0334517180
```

随后完全冻结：

```text
F = A - 0.0334517180 * temporal_ridge_residual
```

验证通过后，时序模型在完整最后 75,000 个训练时点 `813480..888479` 上重训。official test 按时间顺序分块推理，并维护最近 32 个时点的缓存。第一个测试时点使用训练集末尾 32 个时点预热，之后只使用已经出现的测试 feature。

该层使全段分数从 `0.0064109624` 小幅提升到 `0.0064196889`。由于增量只有 `0.0000087265`，它被标记为本地最高分候选，而不是已经确认应替换所有旧版本的正式模型。

### 第 14 步：生成提交并逐层复算

最终顺序推理产生 3,217,458 个预测值，再按 sample submission 的 `row_id` 顺序对齐，只保留：

```text
row_id,target
```

提交审计不只检查文件能否打开，还从保存的中间列逐行复算：

1. 时序层输入的 base prediction 是否与辅助堆叠候选完全一致；
2. `base + temporal_gamma * temporal_signal` 是否等于最终 prediction；
3. gamma 是否为验证阶段冻结的同一个常数；
4. 是否存在 target、weight、responder 等测试禁用输入；
5. CSV 和 ZIP 内的 `submission.csv` 是否字节一致。

最终公式复算最大绝对误差为 `1.14e-16`，属于浮点舍入范围。

## 三、结果如何

### 3.1 各层分数演进

| 模型阶段 | Weighted Zero-Mean R2 | 相对上一阶段绝对提升 |
|---|---:|---:|
| Regime 复合模型 | 0.0060421867 | - |
| + XGBoost 残差 | 0.0061247598 | +0.0000825731 |
| + 辅助状态堆叠残差 | 0.0064109624 | +0.0002862026 |
| + 因果时序 Ridge 残差 | **0.0064196889** | +0.0000087265 |

从原始 Regime 复合模型到最终时序候选，总绝对提升为 `0.0003775023`。

### 3.2 辅助状态堆叠的验证结果

| 区间 | XGBoost 残差基线 | 加辅助状态后 | 提升 |
|---|---:|---:|---:|
| 系数拟合前半段 `868480..878479` | 0.0062386056 | 0.0066227410 | +0.0003841354 |
| 冻结后半段 `878480..888479` | 0.0060127207 | 0.0062025446 | +0.0001898240 |
| outer 全段 | 0.0061247598 | 0.0064109624 | +0.0002862026 |

稳定性检查：

- 3/3 个历史前向折的 full gain 为正；
- 3/3 个历史前向折的 untouched holdout gain 为正；
- 最新 outer 中 5/8 个细时间块提升；
- 11/15 个资产提升；
- 5/5 个 target 绝对值分桶提升。

### 3.3 因果时序层的验证结果

| 区间 | 时序层加入前 | 时序层加入后 | 提升 |
|---|---:|---:|---:|
| 系数拟合段 `868480..872479` | 0.0066136392 | 0.0066157689 | +0.0000021298 |
| 独立 calibration `873480..877479` | 0.0053783237 | 0.0054004117 | +0.0000220879 |
| 最后 holdout `878480..888479` | 0.0062025446 | 0.0062438122 | +0.0000412675 |
| outer 全段 `868480..888479` | 0.0064109624 | **0.0064196889** | +0.0000087265 |

holdout 中 5/8 个细时间块提升。虽然聚合指标和预设门槛全部通过，但提升并非每个时间块都稳定出现，且全段增量很小。

## 四、怎么验证的

### 4.1 使用官方指标

所有选型和复算统一使用官方 Weighted Zero-Mean R2：

```text
Score = 1 - sum(weight * (target - prediction)^2)
            / sum(weight * target^2)
```

全零预测的分数为 0。模型只有在加权误差小于全零基线时才获得正分。
因此 `0.0064196889` 的含义不是“预测准确率为 0.64%”，而是相对于始终预测
0，最终模型把官方加权平方误差降低了约 `0.64197%`。这是一个弱信号、高噪声
任务，所以分数数值本身较小，比较模型时主要看同一时间协议下的增量是否能在
冻结区间复现。

### 4.2 严格时间前推，而不是随机切分

辅助状态模型的主验证协议为：

```text
aux train       688480..776479
aux valid       777480..787479
target train    788480..846479
calibration     847480..867479
outer validate  868480..888479
```

相邻阶段至少保留 1,000 个 `time_id` 的 purge gap。真实 weight/responders 只在更早阶段作为监督使用，target 模型接收的是严格时间外预测值。

时序层又把 outer 切成更严格的三段：

```text
系数拟合      868480..872479
purge gap     872480..873479
独立校准      873480..877479
purge gap     877480..878479
最终 holdout  878480..888479
```

时序系数在最早一段拟合后冻结，不用后续 holdout 反调参数。

### 4.3 official test 只用于推理

official test 的 `time_id=888480..1105919` 没有参与模型选择、系数拟合或分桶调参。最终候选完成了 3,217,458 行顺序推理，CPU 总耗时约 120 秒。

### 4.4 独立审计

2026-08-25 重新执行了本地验证审计和提交结构审计，结果均通过：

- 本地 promotion checks：全部通过；
- CSV 行数：3,217,458；
- 字段：仅 `row_id,target`；
- `row_id`：与 sample submission 顺序完全一致，无重复；
- target：无 NaN/Inf，且不是常数；
- ZIP：仅包含 `submission.csv`，压缩包内外 CSV 一致；
- 最终公式逐行复算最大绝对误差：`1.14e-16`；
- 冻结的时序系数与验证报告完全一致；
- 提交 ZIP SHA256：`ed361d4951c356fe65734e38a3b3bdba036bb16f135b39d5ee021097ee1f926b`。

## 五、目前仍未验证的事项

1. **没有线上排行榜分数**：本地提高不等于公榜或私榜一定提高。
2. **时序层增量很小**：全段只提高 `0.00000873`，应与不含时序层的辅助堆叠候选做线上 A/B。
3. **细分区间并非全部提升**：时序 holdout 为 5/8 个时间块提升，说明仍存在状态漂移。
4. **私榜策略包尚未完成最终验收**：当前已完成公共 test 的顺序 CPU 推理，但还没有把整个四层链路固化为可在 4 核 CPU、12 GB 内存下独立运行的 `main.py/Model.predict()` 私榜策略包。

因此，当前最准确的表述是：

> 四层融合模型是目前项目内严格本地验证分数最高、提交结构已审计通过的候选；它尚未被线上结果或最终私榜运行环境证明为正式最优。

## 六、各步骤对应的代码和证据

### 6.1 Regime 主模型

- 最终重训和融合脚本：`code/experiments/asset_all_tcn/build_latest_regime_submission.py`
- Regime 模型指标：`results/regime_classification_market_75k_probe/metrics.json`
- 三路融合参数：`results/blend_best_panel_market32_75k_cal20k/summary.json`
- 多窗口逐行融合：`results/final_blend_continuous_full_light_constraints_final120k_90k_80k_75k_60k_lgbm/metrics.json`
- 横截面均值调整：`results/final_neutralize_continuous_full_alpha_refined_lgbm/metrics.json`
- Panel 分支：`results/asset_all_panel_market_relative_75k_final/metrics.json`
- Market32 分支：`results/asset_all_market_mean_simple_top32_75k_probe/metrics.json`
- Exact-Market 分支：`results/exact_market_aggregate_75k_probe/metrics.json`

### 6.2 XGBoost 残差基线

- 融合脚本：`code/experiments/asset_all_tcn/blend_regime_with_xgb_residual.py`
- 稳定推荐提交：`results/reproduce_wsl_xgb_residual_20260824_final/submission.zip`
- 复现后的完整指标：`results/reproduce_wsl_xgb_residual_20260824_final/source_metrics.json`
- 独立审计：`results/reproduce_wsl_xgb_residual_20260824_final/audit_report.json`

### 6.3 辅助状态堆叠

- 训练脚本：`code/experiments/asset_all_tcn/train_auxiliary_stacking.py`
- 历史折和最新 outer 审计：`results/auxiliary_stacking_audit_20260824/audit_report.json`
- 不含时序层的候选提交：`results/auxiliary_stacking_candidate_20260824/submission.zip`
- 候选结构审计：`results/auxiliary_stacking_candidate_20260824/audit_report.json`

### 6.4 因果时序 Ridge

- 时序特征和模型协议：`code/experiments/asset_all_tcn/final_residual_train_predict_ts_features.py`
- 时序验证脚本：`code/experiments/asset_all_tcn/audit_temporal_ridge_candidate.py`
- 全量顺序推理脚本：`code/experiments/asset_all_tcn/build_temporal_ridge_candidate.py`
- 提交审计脚本：`code/experiments/asset_all_tcn/audit_temporal_submission.py`
- 当前本地最高分提交：`results/temporal_ridge_candidate_20260824/submission.zip`
- 当前本地最高分验证审计：`results/temporal_ridge_candidate_audit_20260824/audit_report.json`
- 当前本地最高分提交审计：`results/temporal_ridge_candidate_20260824/audit_report.json`
- 时序模型文件：`models/temporal_ridge_candidate_20260824/temporal_ridge_model.pkl`

### 6.5 综合研究记录

- Feature、weight、target、辅助状态和时序实验记录：`docs/feature_weight_target_analysis_20260824.md`
